import React from 'react'
import './styles.scss'

const IconCircle = ({ src }) => (
  <div className="containerIcon d-flex justify-content-center align-items-center">
    <img src={src} />
  </div>
)

export default IconCircle
